<h1><%= @patient.name %></h1>

<h2>You have <%= pluralize(@patient.appointments.count, "appointment") %>:</h2>

<ol>
<% @patient.appointments.each do |appt| %>
    <li> <ul>
        <li>Doctor: <%= link_to appt.doctor.name, doctor_path(appt.doctor) %></li>
        <li><%= appt.date %></li>

    </ul>
    </li>

<% end %>
</ol>